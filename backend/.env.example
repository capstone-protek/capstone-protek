# ==========================================
# 📝 CONTOH KONFIGURASI ENVIRONMENT (TEMPLATE)
# ==========================================
# Instruksi:
# 1. Copy file ini dan rename menjadi ".env"
# 2. Isi value-nya dengan kredensial asli (Local atau Railway)
# 3. JANGAN commit file ".env" asli ke GitHub
# ==========================================

# --- 1. SERVER CONFIGURATION ---
# Port default backend berjalan
PORT=4000

# --- 2. DATABASE CONNECTION (Prisma) ---
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
#
# Opsi A: Koneksi ke Localhost (Jika DB ada di laptop)
# DATABASE_URL="postgresql://postgres:root@localhost:5432/protek_db?schema=public"
#
# Opsi B: Koneksi ke Railway (Untuk Tim ML Inject Data / Migrasi)
# Ambil dari: Dashboard Railway -> Service DB -> Connect -> Public Networking
DATABASE_URL="postgresql://postgres:password_rahasia@roundhouse.proxy.rlwy.net:12345/railway"

# --- 3. ML API INTEGRATION ---
# URL tempat API Python (FastAPI) berjalan
# Default Local: http://localhost:8000/predict
# Default Railway: https://coba-deploy-ml-production.up.railway.app/predict
ML_API_URL="https://capstone-protek-production.up.railway.app/predict"

# --- 4. CHATBOT (Opsional) ---
# Jika menggunakan fitur Chatbot (sesuaikan dengan LLM yang dipake)
GEMINI_API_KEY="isi_api_key_disini"